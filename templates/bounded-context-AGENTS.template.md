# Local AGENTS.md — <Bounded Context / Module>

This file refines the repository root `AGENTS.md` for this subtree. Do not duplicate root rules.

## Responsibility

`<What this context/module owns.>`

## Boundaries

### Owns
- `<data/behavior>`

### Does not own
- `<explicit non-responsibility>`

## Allowed dependencies

- `<dependency/context>` — `<why/how>`

## Forbidden dependencies

- `<dependency/context>` — `<reason>`

## Public contracts

- `<API/event/interface/schema>`

## Local invariants

- `<invariant>`

## Required validation

- `<command>`

## Local documentation

- `<authoritative path>`

## Stop conditions

Raise an architecture blocker before implementation when:

- `<module-specific material ambiguity>`
- `<boundary change requires explicit decision>`